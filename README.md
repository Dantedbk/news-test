# News app with comments

  This is a code bundle for News app with comments. The original project is available at https://www.figma.com/design/sXWY800VypaI0x3AYGewOo/News-app-with-comments.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
